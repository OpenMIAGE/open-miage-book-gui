var OpenM_Book_Gui_UserDAO = {
    'users': new Array(),
    'get': function(user_id){
       //verif if user is load, sinon récup via ajax
       
       var user = {
           'name':'',
       };
       return user;
    },
    'set': function(user){
        //verrif chaque champ du user, si il est different de celui en mémoire.
        
    }
}